name: revlog
description: A car maintenance tracker
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  file_picker: ^9.2.1
  google_fonts: ^6.2.1
  pdf: ^3.10.8
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true